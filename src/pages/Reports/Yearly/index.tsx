import React from 'react';
import { Card } from 'antd';

const Yearly: React.FC = () => {
    return (
        <Card title="Yearly">
            <p>Welcome to the Yearly!</p>
        </Card>
    );
};

export default Yearly;
